# Marvel Heroes Omega Overlay — releases

Binary releases only (zip + SHA256). Download **MarvelHeroesOmega-Overlay-v*-win-x64.zip** from [Releases](https://github.com/wmascent/MarvelHeroesOmega.Overlay.releases/releases).

GitHub always lists auto-generated “Source code” archives on tagged releases; this repo uses `.gitattributes` so those archives are empty. Ignore them and use the uploaded zip asset.
